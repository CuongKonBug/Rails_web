<%= form_with(model: danhmuc) do |form| %>
  <% if danhmuc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(danhmuc.errors.count, "error") %> prohibited this danhmuc from being saved:</h2>

      <ul>
        <% danhmuc.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tendanhmuc,for:"exampleInputEmail1", class:"form-label" %>
    <%= form.text_field :tendanhmuc,class:"form-control",style:"width:700px" %>
  </div>

  <div class="actions">
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>
